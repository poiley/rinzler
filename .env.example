# ===================================
# TIMEZONE CONFIGURATION
# ===================================
# Timezone for all containers - affects logging timestamps and scheduled tasks
# Used by: Plex, Pihole, Wireguard, Jackett, Radarr, Sonarr, Lidarr, HomeAssistant, etc.
# Format: Region/City (see https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=America/Los_Angeles

# ===================================
# USER/GROUP IDS
# ===================================
# Linux user and group IDs for file permissions inside containers
# Used by: All LinuxServer.io containers (Plex, Jackett, Radarr, Sonarr, Lidarr, etc.)
# Find your IDs with: id -u (for PUID) and id -g (for PGID)
PUID=1000
PGID=1000

# ===================================
# NETWORK CONFIGURATION
# ===================================
# Wireguard server URL for client configuration
# Used by: Wireguard container
# Set to "auto" to automatically detect public IP, or specify your domain/IP
SERVERURL=auto

# Local server IP address
# Used by: Multiple services for internal communication
# This is your server's LAN IP address
LOCAL_IP=192.168.1.100

# ===================================
# WIREGUARD VPN CONFIGURATION
# ===================================
# Private key from your Mullvad WireGuard configuration file
# Used by: Torrent stack (docker-compose.torrent_stack.yml) for VPN tunnel
# Get from: https://mullvad.net/en/account/wireguard-config
WIREGUARD_PRIVATE_KEY=your_private_key_here

# VPN IP address assigned by Mullvad
# Used by: Torrent stack for VPN interface configuration
# Format: IP/CIDR (e.g., 10.64.197.249/32)
WIREGUARD_ADDRESSES=10.64.0.0/32

# VPN provider name
# Used by: Torrent stack - determines VPN configuration format
VPN_SERVICE_PROVIDER=mullvad

# VPN protocol type
# Used by: Torrent stack - can be "wireguard" or "openvpn"
VPN_TYPE=wireguard

# ===================================
# PIHOLE CONFIGURATION
# ===================================
# Admin password for Pihole web interface
# Used by: Pihole container (docker-compose.pihole.yml)
# Access at: http://YOUR_IP:8081/admin
# IMPORTANT: Change this to a secure password!
PIHOLE_PASSWORD=secure_password_here

# Rate limiting for DNS queries (queries/time)
# Used by: Pihole for preventing DNS amplification attacks
# Format: "queries/seconds" or "0/0" to disable rate limiting
PIHOLE_RATE_LIMIT=0/0

# ===================================
# BASIC AUTHENTICATION
# ===================================
# Base64 encoded username:password for services requiring basic auth
# Used by: Services exposed through Traefik or requiring HTTP basic auth
# Generate with: echo -n 'username:password' | base64
# IMPORTANT: Replace with your own credentials!
BASIC_AUTH_HEADER=Basic base64_encoded_credentials_here

# ===================================
# PLEX CONFIGURATION
# ===================================
# Plex account username/email
# Used by: Plex container for server ownership and remote access
PLEX_MEDIA_SERVER_USER=your.email@example.com

# URLs where Plex can be accessed (internal and external)
# Used by: Plex for remote access configuration
# Format: comma-separated list of URLs
# Note: Update the IP to match your LOCAL_IP value
ADVERTISE_IP=http://192.168.1.100:32400,https://yourdomain.duckdns.org:32400

# Preferred port for Plex connections
# Used by: Plex for incoming connections
PLEX_PREFER_PORT=32400

# ===================================
# DUCKDNS CONFIGURATION
# ===================================
# DuckDNS subdomain (without .duckdns.org)
# Used by: DuckDNS container (docker-compose.duckdns-kavita-caddy.yml)
# This will create: SUBDOMAIN.duckdns.org
SUBDOMAINS=yoursubdomain

# DuckDNS API token for dynamic DNS updates
# Used by: DuckDNS container for updating your IP
# Get from: https://www.duckdns.org/domains
# IMPORTANT: Keep this secret!
TOKEN=your-duckdns-token-here

# ===================================
# SAMBA CONFIGURATION
# ===================================
# Samba share configuration string
# Used by: Samba container (docker-compose.samba.yml)
# Format: name;path;browsable;readonly;guest;users;admins;writelist
SHARE=mount;/mount;yes;no;yes;all;none;none

# Windows workgroup name
# Used by: Samba for NetBIOS/SMB networking
WORKGROUP=WORKGROUP

# ===================================
# APPLICATION SETTINGS
# ===================================
# Enable automatic updates for LinuxServer.io containers
# Used by: Jackett, Radarr, Sonarr, Lidarr, etc.
# Set to "false" to disable automatic updates
AUTO_UPDATE=true

# File creation mask (Unix permissions)
# Used by: Various containers for new file permissions
# 022 = owner:rwx, group:r-x, others:r-x
UMASK=022

# ===================================
# CAPTCHA SOLVER CONFIGURATION
# ===================================
# Captcha solving service for FlareSolverr
# Used by: FlareSolverr container (docker-compose.flaresolverr.yml)
# Options: "none", "hcaptcha-solver", "recaptcha-solver", etc.
CAPTCHA_SOLVER=none

# Logging verbosity
# Used by: FlareSolverr and other services supporting log levels
# Options: "debug", "info", "warn", "error"
LOG_LEVEL=info

# Enable HTML response logging
# Used by: FlareSolverr for debugging
# Set to "true" to log full HTML responses (can be very verbose)
LOG_HTML=false

# ===================================
# SERVICE AUTHENTICATION
# ===================================
# Authentication method for Radarr API
# Used by: Radarr container
# 0 = None, 1 = Basic, 2 = Forms
RADARR__AUTHENTICATION_METHOD=1

# Authentication method for Sonarr API
# Used by: Sonarr container
# 0 = None, 1 = Basic, 2 = Forms
SONARR__AUTHENTICATION_METHOD=1

# Authentication method for Lidarr API
# Used by: Lidarr container
# 0 = None, 1 = Basic, 2 = Forms
LIDARR__AUTHENTICATION_METHOD=1

# URL base for Radarr (for reverse proxy)
# Used by: Radarr when behind a reverse proxy
# Allows access at: http://YOUR_IP/radarr
RADARR__URLBASE=/radarr

# URL base for Sonarr (for reverse proxy)
# Used by: Sonarr when behind a reverse proxy
# Allows access at: http://YOUR_IP/sonarr
SONARR__URLBASE=/sonarr

# URL base for Lidarr (for reverse proxy)
# Used by: Lidarr when behind a reverse proxy
# Allows access at: http://YOUR_IP/lidarr
LIDARR__URLBASE=/lidarr

# URL base for Tautulli (for reverse proxy)
# Used by: Tautulli when behind a reverse proxy
# Allows access at: http://YOUR_IP/tautulli
TAUTULLI__URLBASE=/tautulli

# ===================================
# WIREGUARD PEERS CONFIGURATION
# ===================================
# Comma-separated list of peer names for Wireguard server
# Used by: Wireguard container to generate client configs
# Each peer gets its own configuration file
PEERS=mac,iphone,backup

# DNS server for Wireguard peers
# Used by: Wireguard container - set in peer configurations
# Usually points to Pihole for ad-blocking
PEERDNS=192.168.1.100

# ===================================
# LOCATION SETTINGS
# ===================================
# Preferred server cities for VPN connections
# Used by: VPN services that support location selection
SERVER_CITIES=Seattle WA

# Preferred server countries for VPN connections
# Used by: VPN services that support location selection
SERVER_COUNTRIES=USA

# ===================================
# TRANSMISSION CONFIGURATION
# ===================================
# Web UI theme for Transmission
# Used by: Transmission in torrent stack
# Options: "transmission-web-control", "combustion", "kettu", etc.
TRANSMISSION_WEB_UI=transmission-web-control

# ===================================
# FMD2 CONFIGURATION
# ===================================
# File type for manga transfers
# Used by: FMD2 container (docker-compose.fmd2.yml)
# Common formats: .cbz, .pdf, .epub
TRANSFER_FILE_TYPE=.cbz

# Time threshold for processing
# Used by: FMD2 for batch processing delays
THRESHOLD_MINUTES=3

# ===================================
# VSCODE CONFIGURATION
# ===================================
# Default workspace directory
# Used by: VSCode Server container (docker-compose.vscode.yml)
# Path inside the container where projects are stored
DEFAULT_WORKSPACE=/config/workspace

# ===================================
# DOCKER/SERVICE SETTINGS
# ===================================
# Browser automation driver
# Used by: Services requiring browser automation
# Options: "chrome", "firefox", "nodriver"
DRIVER=nodriver

# DNS over TLS
# Used by: Services supporting encrypted DNS
# "on" enables DoT support
DOT=on