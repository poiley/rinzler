apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-server
  namespace: mcp-servers
  labels:
    app: k8s-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: k8s-mcp-server