apiVersion: v1
kind: Service
metadata:
  name: simple-k8s-mcp
  namespace: mcp-servers
  labels:
    app: simple-k8s-mcp
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: simple-k8s-mcp