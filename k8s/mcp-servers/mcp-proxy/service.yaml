apiVersion: v1
kind: Service
metadata:
  name: mcp-proxy
  namespace: mcp-servers
  labels:
    app: mcp-proxy
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mcp-proxy