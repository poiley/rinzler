apiVersion: v1
kind: Secret
metadata:
  name: autokuma-secret
  namespace: monitoring
type: Opaque
stringData:
  # Default admin password - must match the hash in deployment.yaml init container
  admin-password: "password"